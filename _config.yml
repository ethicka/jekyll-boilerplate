title:
description:
baseurl: ""
url: ""
jquery: true # default

# Assets
assets:
  sources:
    - node_modules
    - assets/pdf
  autoprefixer:
    browsers:
      - "last 2 versions"

# Build settings
markdown: kramdown
plugins:
  - jekyll-assets
  - uglifier

include:
#  - _headers # Netlify headers

# Exclude
exclude:
  - exclude
  - readme.md
  - Gemfile
  - Gemfile.lock
  - node_modules
  - yarn.lock
  - package.json
